# SPDX-License-Identifier: Apache-2.0
name: Release Charts

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  release:
    uses: lpasquali/rune-ci/.github/workflows/helm-release.yml@d1d2d2863dac135812bf98b89ae6f9bef8c33e63
    with:
      charts-dir: "charts"
