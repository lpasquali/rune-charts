# SPDX-License-Identifier: Apache-2.0
name: Quality Gates

on:
  pull_request:
    branches: [main, develop]
  push:
    branches: [main, develop]

permissions:
  contents: read
  pull-requests: write
  id-token: write
  attestations: write
  packages: write

jobs:
  security:
    uses: lpasquali/rune-ci/.github/workflows/security-scan.yml@9f939b2c28317b6f55c1913c6a6485bd91e1bda5 # main

  helm:
    uses: lpasquali/rune-ci/.github/workflows/helm-quality.yml@9f939b2c28317b6f55c1913c6a6485bd91e1bda5 # main
    with:
      chart-dirs: "charts/postgres charts/rune charts/rune-operator"

  crossplane:
    name: crossplane / Validate XRDs and Compositions
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - name: Install Crossplane CLI
        run: |
          curl -sL https://raw.githubusercontent.com/crossplane/crossplane/main/install.sh | sh
          sudo mv crossplane /usr/local/bin/crossplane
      - name: Validate XRDs and Compositions
        run: crossplane beta validate crossplane/xrds/ crossplane/compositions/ 2>&1 || echo "Compositions not yet available in Phase 0"
      - name: Install yamllint
        run: pip install --quiet yamllint
      - name: yamllint crossplane directory
        run: yamllint --strict crossplane/

  integration:
    name: integration / Deployment Test (K8s ${{ matrix.k8s-version }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        k8s-version:
          - "1.34.3"
          - "1.35.0"
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - name: Build rune chart dependency
        run: helm dependency build charts/rune
      - name: Start kind
        uses: helm/kind-action@a1b0e391336a6ee6713a0583f8c6240d70863de3 # v1.12.0
        with:
          node_image: kindest/node:v${{ matrix.k8s-version }}
          wait: 120s
      - name: Test Charts
        run: |
          for chart in postgres rune rune-operator; do
            echo "Testing chart: ${chart}"
            install_args=(--namespace rune-test --create-namespace --wait --timeout=2m)
            if [ "${chart}" = "postgres" ]; then
              install_args+=(--set auth.password=test-password --set persistence.enabled=false)
            fi
            if ! helm install "${chart}" "./charts/${chart}" "${install_args[@]}"; then
              kubectl get pods -n rune-test -o wide || true
              kubectl describe pods -n rune-test || true
              kubectl logs -n rune-test -l app.kubernetes.io/instance="${chart}" --all-containers=true --tail=-1 || true
              exit 1
            fi
            kubectl get all -n rune-test
            helm uninstall "${chart}" --namespace rune-test
          done

  compliance:
    needs: [security, helm, crossplane, integration]
    if: always()
    uses: lpasquali/rune-ci/.github/workflows/pr-compliance.yml@9f939b2c28317b6f55c1913c6a6485bd91e1bda5 # main
    with:
      needs-json: ${{ toJson(needs) }}
      merge-gate-excludes: "helm,integration,security,crossplane" # Force pass until Phase 1a
